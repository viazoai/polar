services:
  polar-api:
    build: ./backend
    container_name: polar-api
    volumes:
      - ./backend:/app
      - ./data:/app/data
    ports:
      - "3201:8000"
    environment:
      - TZ=Asia/Seoul
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - SECRET_KEY=${SECRET_KEY:-polar-dev-secret}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped

  polar-web:
    build: ./frontend
    container_name: polar-web
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "3200:5173"
    environment:
      - TZ=Asia/Seoul
    command: npm run dev -- --host
    restart: unless-stopped
