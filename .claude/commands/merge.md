Run the following commands sequentially using the Bash tool:
1. `git push`
1. `git checkout main`
2. `git merge dev`
3. `git push`
4. `git checkout dev`

After each command, show the output. If any command fails, stop and report the error.
